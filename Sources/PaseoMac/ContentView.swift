import SwiftUI

/// Entry view. The redesigned UI lives in `RootView` (design/v1); the prior
/// NavigationSplitView implementation is superseded.
struct ContentView: View {
    var body: some View {
        RootView()
    }
}
